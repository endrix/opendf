import java.io.File;
import java.util.Scanner;

actor art_Source_txt(string fileName) ==> Out:

        File input:=File(fileName);
        Scanner scanner:=Scanner(input);
        
	action ==> [scanner.nextInt()]
        guard scanner.hasNextInt()
	end
end
